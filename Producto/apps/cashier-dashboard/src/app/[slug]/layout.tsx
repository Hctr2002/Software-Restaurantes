import CashierThemeWrapper from "../CashierThemeWrapper";

export default function SlugLayout({ children }: { children: React.ReactNode }) {
  return <CashierThemeWrapper>{children}</CashierThemeWrapper>;
}
