import WaiterThemeWrapper from "../WaiterThemeWrapper";

export default function SlugLayout({ children }: { children: React.ReactNode }) {
  return <WaiterThemeWrapper>{children}</WaiterThemeWrapper>;
}
